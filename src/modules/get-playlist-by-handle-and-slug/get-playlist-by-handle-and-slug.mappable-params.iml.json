[
  // Defines "id" as mandatory module input parameter.
  {
    "name": "handle", // Makes value accesible via "{{parameters.id}}".
    "type": "text", // Sets the type to text.
    "label": "User Handle", // Sets the user friendly label visible in the module.
    "required": true // Sets the parameter as mandatory.
  },
  {
    "name": "slug", // Makes value accesible via "{{parameters.id}}".
    "type": "text", // Sets the type to text.
    "label": "Playlist Slug", // Sets the user friendly label visible in the module.
    "required": true // Sets the parameter as mandatory.
  }
]
