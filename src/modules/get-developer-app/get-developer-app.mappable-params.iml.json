[
  // Defines "id" as mandatory module input parameter.
  {
    "name": "address", // Makes value accesible via "{{parameters.id}}".
    "type": "text", // Sets the type to text.
    "label": "Developer app address (API Key)", // Sets the user friendly label visible in the module.
    "required": true // Sets the parameter as mandatory.
  }
]
