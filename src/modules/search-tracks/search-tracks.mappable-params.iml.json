// Defines "search" and "limit" as module input parameters.
[
	{
		"name": "search",                                                     // Makes value accesible via "{{parameters.search}}".
		"type": "text",                                                       // Sets the type to text.
		"label": "Search",                                                    // Sets the user friendly label visible in the module
		"help": "The string to use in the fulltext search, e.g. `John Doe`."  // Sets the help text with an example under input field in the module.
	},
	{
		"name": "limit",
		"type": "number",
		"label": "Limit",
		"default": 10,
		"help": "Maximum number of returned results (bundles)."
	}
]